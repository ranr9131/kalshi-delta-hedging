# Secrets
live/.env
live/.env.*
!live/.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# Data / cache (large files, regeneratable)
data/cache/
# Ignore everything under data/logs/ except the precomputed lookup table
# the trader depends on at runtime
data/logs/*
!data/logs/minute_analysis_2d.csv

# Logs
*.log

# Local trader state (per-machine, regenerated at runtime)
live/*.csv

# Fair-value (v3) pipeline data & scratch outputs (large / regeneratable)
live/fv/*.csv
live/fv/*.txt
live/fv/*.out
live/fv/basis_report.json

# IDE
.idea/

# Scratch / handoff notes
CONTEXT_HANDOFF.md

# OS
.DS_Store
Thumbs.db

# market-data recordings (too big for git; corpus lives on AWS/disk)
recordings/
recordings_fresh/
